class Log {
  constructor(id, house, professor, isAdd, value) {
    this.id = id
    this.house = house,
    this.professor = professor,
    this.date = new Date(),
    this.isAdd = isAdd,
    this.value = value
  }
}

export default Log
